#include <iostream>
using namespace std;

int main() {
	int t;
	cin >> t;
	for (int i = 0; i < t; i++) {
		int a, b, temp = 1;
		cin >> a >> b;
		for (int j = 0; j < b; j++)
			temp = temp * a % 10;
		if (!temp)
			cout << "10" << endl;
		else
			cout << temp << endl;
	}
}
