#include <bits/stdc++.h>
using namespace std;

int main(void) {
	int n, a;
	bool b = true;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a;
		if (abs(a - i) % 2) {
			b = false;
			break;
		}
	}
	if (b)
		cout << "YES" << endl;
	else
		cout << "NO" << endl;
}
